# For more information about this configuration visit
# https://docs.fastlane.tools/actions/scan/#scanfile

# In general, you can use the options available
# fastlane scan --help

# Remove the # in front of the line to enable the option

# scheme("Example")

# open_report(true)

# clean(true)

# Enable skip_build to skip debug builds for faster test performance
skip_build(false)
clean(true)
#build_for_testing(true)
scheme("SurveyStaging") # projects scheme name
workspace("Survey.xcworkspace")
output_directory("./fastlane/derived_data/Logs/Test")
derived_data_path("./fastlane/derived_data")
result_bundle(true)
code_coverage(true)
